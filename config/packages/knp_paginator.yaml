knp_paginator:
  page_range: 2 # Nombre de pages affichées dans la barre de pagination
  default_options:
    page_name: page # Nom du paramètre de requête pour la page
    sort_field_name: sort # Nom du paramètre de requête pour le tri
    sort_direction_name: direction # Nom du paramètre de requête pour la direction de tri
    distinct: true # Si true, la pagination est effectuée sur des éléments distincts
  template:
    pagination: "@KnpPaginator/Pagination/materialize_pagination.html.twig" # sliding pagination controls template
    sortable: "@KnpPaginator/Pagination/twitter_bootstrap_v3_sortable_link.html.twig" # sort link template
    filtration: "@KnpPaginator/Pagination/bootstrap_v5_filtration.html.twig"
